{
  "name": "nurae-runtime",
  "version": "1.0.0",
  "private": true,
  "description": "NURAE V00.00.000-beta-01 — isolated bot runtime service (BotManager + Telegram long-poll + AI providers)",
  "scripts": {
    "dev": "bun --hot ./index.ts",
    "start": "bun ./index.ts"
  }
}
