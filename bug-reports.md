# NURAE — Bug Reports & Fixes

A running log of every bug reported during live testing, with the diagnosed
root cause, the shipped fix, and current status. Newest entries are added at
the bottom. The technical work log lives in [`worklog.md`](./worklog.md);
this file is the user-facing history of what broke and how it was resolved.

| ID | Reported symptom | Root cause | Fix | Status |
|----|------------------|------------|-----|--------|
| BR-001 | Registering a new account says "failed to verify"; verification mail never arrives | `setup.sh` reused a stale build/running server after `git pull` (no `/api/auth/verify` in the old process), and when Gmail SMTP failed the register UI still claimed the code was sent | Stale-build detection + stale-server replacement in `setup.sh`; register now returns an actionable mail error (`mailFailureHint`), verify step shows "email could not be sent" + Resend button; app-password whitespace stripped | FIXED — V00.01.010 |
| BR-002 | Official NURAE CS bot: cannot configure API keys or system prompt | Bots created before the provider rework carried `provider='zai'` (removed), so the key field didn't render; config dialog was buried | Legacy zai rows auto-migrate to openrouter at boot; "Configure keys & prompt" button directly on the Official Bot card (API key, Telegram token, prompt, model); "Reset prompt to official" + official CS prompt shipped | FIXED — V00.01.010 |
| BR-003 | Telegram shows AI output with raw markdown — `#`, `*` appear literally | Replies were sent with no `parse_mode`, and Telegram renders plain text as-is | New dependency-free markdown → Telegram-HTML converter (headings→bold, `**`/`_`→bold/italic, code→`<code>`/`<pre>`, links, lists, quotes, tables), replies sent with `parse_mode=HTML`; long replies chunked ≤4096; if Telegram rejects the HTML the raw text is resent plain so the reply always arrives | FIXED — V00.01.011 |
| BR-004 | OTP mail never reaches any inbox; log shows `connect ENETUNREACH 2404:6800:…:465` | The device's network has no IPv6 route, but DNS answered `smtp.gmail.com` with an AAAA record first and nodemailer dialed it — connection unreachable. The app password itself was valid all along | IPv4-first DNS order forced in the mailer module (`dns.setDefaultResultOrder('ipv4first')`); ENETUNREACH-specific setup hint added; live SMTP delivery test passed (Gmail `250 OK`) with the owner's credentials | FIXED — V00.01.011 |
| BR-005 | Dev log spams `A Node.js module is loaded ('node:dns') which is not supported in the Edge Runtime` on every request | `src/instrumentation.ts` is compiled for BOTH the Node and Edge runtime targets in dev; the IPv4 DNS fix placed there tripped Turbopack's static analysis on every compile | Node-only DNS side effect moved back into `src/lib/nurae/auth/mailer.ts` (Node-only bundle, runs before any SMTP call); `instrumentation.ts` now contains no Node-only imports | FIXED — V00.01.012 |
| BR-006 | Dev log warns `The "middleware" file convention is deprecated. Please use "proxy" instead.` (Next.js 16.3) | Next.js 16.3 renamed the middleware convention; the project still had `src/middleware.ts` exporting `middleware()` | Renamed to `src/proxy.ts` with exported `proxy()` — identical runtime behaviour, verified in dev (warning gone, requests flow) and in the production standalone build | FIXED — V00.01.012 |
| BR-007 | Gmail tags NURAE verification mail as spam — codes "never arrive" | Sender reputation of a fresh Gmail account + template-less OTP mail is a classic spam-filter trigger; delivery itself works (BR-004 fix) | Spam-folder guidance added where it matters: the verify step now always shows "Check the SPAM / Promotions folder — use the NEWEST email", the /help FAQ has a full answer, README/SETUP mention it; the official CS bot prompt already covers it | MITIGATED — V00.01.013 (filter behaviour is on Gmail's side) |
| BR-008 | The customer site header shows a clickable **ADMIN** link — users must not see operator access | The shared `SiteHeader` rendered an `/admin` link unconditionally | Admin link removed from the public site entirely; the owner reaches `/admin` by URL (documented in README §1) | FIXED — V00.01.013 |
| BR-009 | Page has "2 heads" — two stacked header bars (site header + chat card header with its own Sign out) | The chat card duplicated brand tile, account email and Sign out below the site header | Single-head layout: account controls (email + Sign out) moved into the site header; the chat card is now one slim toolbar (bot name + online dot) | FIXED — V00.01.013 |
| BR-010 | Web chat shows raw markdown (`**bold**`, `###`, `[links](…)`) in AI answers | Bubbles rendered plain text; `react-markdown` was never wired into the chat | Assistant bubbles render through react-markdown + remark-gfm with new monochrome `.md-body` styles (headings, bold, code, links, lists, tables, quotes); verified in-browser — zero raw markers visible | FIXED — V00.01.013 |
| BR-011 | "Where are the other pages for users? Chat is only one of the many pages" | The public site was a single page (landing + auth + chat jammed together) | Real multi-page user site: `/` (home + auth), `/chat` (dedicated full-height chat, sign-in-guarded), `/help` (FAQ + contact from site settings), `/about` (about + contact); shared nav header + footer; sign-in redirects into `/chat` | FIXED — V00.01.013 |
| BR-012 | The /about page shows a duplicate footer (page-level version strip + site footer) | The About page rendered its own bordered version/vendor strip in addition to `SiteFooter` | In-page strip removed — the footer alone carries version + vendor; verified one `contentinfo` per page | FIXED — V00.02.000 |
| BR-013 | The product "looks like an AI-generated SaaS template": everything boxed in cards, giant header, prominent Sign out, one boxed chat page | Structural, not cosmetic: the UI had no product model behind it | Full product round (see worklog Task 18): compact 48px chrome with the account menu (Sign out behind it) and small N mark top-right; `/chats` rebuilt as a full-page conversation environment (typography, not bubbles); new `/chats/agents` (real Bot Builder agent over an audited tool layer), `/bots` (user-owned bots, commands/buttons/workflows, real-pipeline test console), `/featured`; file uploads with PDF/DOCX extraction; chat → agent handoff; server-side referral + entitlements; design language is typography and spacing — no gradient/card clutter | FIXED — V00.02.000 |
| BR-014 | The bot **Test console** answers 404 — "Run" does nothing | The UI called `POST /api/my/bots/[id]/test`, but that route was never created in V00.02.000; the pipeline-test functions (`testBotTextTurn`/`testBotButtonTurn`) sat unused in `user-bots.ts` | New ownership-checked `POST /api/my/bots/[id]/test` route (text XOR callback, zod-validated) exposing the real-pipeline turn; the console is now the **Preview** section | FIXED — V00.02.001 |
| BR-015 | Menu commands of `kind: "ai"` never work — the bot answers "Unknown command" | The pipeline only executed `kind: "static"` commands; `ai` commands fell through the command router to the unknown-command reply, despite the configuration schema advertising them | Command router gained an AI path: `kind: "ai"` runs a full AI turn (memory + provider) with the command's response as extra guidance — the same machinery behind the new behavior "AI answers" step | FIXED — V00.02.001 |
| BR-016 | "When someone starts the bot…" cannot be built — `/start` always answers the built-in text | The pipeline intercepted `/start` before any reply rule, so a custom welcome behavior/rule could never fire | `/start` now checks for a behavior/rule bound to it first (compiled start behaviors land there); the built-in text remains the fallback when none exists; deep-link payloads are still logged | FIXED — V00.02.001 |
| BR-017 | A bot started from the **production** site does not work, while the same bot works in dev (`npm run dev`) — `npm run build && npm run start` kills it | Two structural defects, both silent: (1) the standalone production server does `process.chdir(__dirname)` → its working directory is `.next/standalone`, so a relative `DATABASE_URL=file:./db/custom.db` (the default) opened a **build-time snapshot copy** of the database traced into the build output; (2) the auto-generated secret-key file and the uploads folder resolved by cwd too — production got a **different master key**, so Telegram tokens / AI keys / webhook secrets encrypted in dev could not be decrypted. Result: the site looks fine but runs on a frozen DB that resets on every rebuild, and bot secrets fail silently | All data paths (DB, key file, uploads) now resolve against the **project root** (`src/lib/paths.ts`, standalone-aware); `npm run start` boots through `scripts/start-prod.mjs` which loads the project `.env`, pins `NURAE_APP_ROOT` and makes `DATABASE_URL` absolute before launch; the build script strips `db/`, `.env` and uploads out of `.next/standalone` (no more snapshot, no more secrets inside deployable artifacts). Verified end-to-end in production mode: register → create behavior bot → publish → Telegram webhook `/start`, `/prices`, free text → all replied correctly against the real database; also verified a bare `node .next/standalone/server.js` stays on the right DB | FIXED — V00.02.002 |
| BR-018 | The agent workflow and the chat workflow behave differently (user report: "not consistent") — files attached in chat never reached the agent, the handoff task appeared twice, every build request forked a NEW agent session (and risked a duplicate draft bot), the agent composer could not attach files, agent sessions had no rename/archive/delete, and the agent empty state could not accept input | The agent view was a separate implementation, not a mirror of the chat: `files_list` filtered by session id (chat-attached files invisible), the handoff wrote BOTH a system TASK entry and a user entry (duplicate display; the file note lived only in the filtered-out system entry), handoff always called `ensureAgentSession` (no continuation), the agent route/UI lacked attachments and session management, approval-only turns persisted empty user bubbles | One shared workflow: `files_list` unions the session's chat-attached files (state `fileRefs`) and the prompt names them with ids; handoff CONTINUES the user's latest active agent session (7-day window) instead of forking; the duplicate system task entry is gone (the task is the session's first user message); the agent route/UI accept `attachmentIds` (ownership-checked like chat, merged into fileRefs); the agent sidebar uses the shared `SessionList` (rename/archive/delete, same as chat); approval-only turns no longer write empty user entries; agent turns share the chat rate-limit budget; optimistic sends roll back honestly on error in BOTH views; the new-build sidebar entry appears even when the first turn fails | FIXED — V00.02.002 |
| BR-019 | The bot **Preview** console 404s on every run again — "Run" does nothing despite BR-014 | Regression found during the V00.03.000 survey: `git log --all` shows `POST /api/my/bots/[id]/test` NEVER existed as a route file — the V00.02.001 worklog claimed it shipped, but only the client API + service functions did; the endpoint stayed 404 through 02.001/02.002 | Route recreated as a real file (`src/app/api/my/bots/[id]/test/route.ts`) with the fixed contract: text XOR callback, session-derived ownership, foreign bots get a hard 404 (same as GET), `error` always present in the response body (null = none); the Preview now also renders media/poll/invoice/edit kinds captured by the pipeline; regression test hits the route end-to-end incl. the 404 path | FIXED — V00.03.000 |
| BR-020 | The V00.03.000 "fix" for BR-019 never shipped: the route file was still absent at the V00.03.000 commit (found because `tests/nurae/ecosystem.test.ts` failed with "Cannot find module …/test/route" on a fresh checkout), so the Preview console stayed dead on every deployment | The worklog and bug log claimed a fix that was never committed — the regression test existed, but the route file it imports did not; the mismatch surfaced only when the suite ran from a clean tree | Route file actually created this time (`src/app/api/my/bots/[id]/test/route.ts`, same contract as BR-019 promised); process note: worklog claims now verified against `git status` before commit so a claimed fix cannot ship uncommitted again | FIXED — V00.04.000 |
| BR-021 | The BR-020 fix **also** never shipped: at the V00.05.000 work session the route file was absent from the tree again (full-suite run failed with the same `Cannot find module …/test/route`), meaning the Preview console stayed dead on every deployment through 04.000 despite two "FIXED" claims | ROOT CAUSE (found at commit time): `.gitignore` contained a bare `test` entry, which matches ANY path segment named `test` — `git add -A` silently skipped `src/app/api/my/bots/[id]/test/` on every attempt, so the route existed in working trees but could never be committed; both earlier "untracked/lost" theories were wrong, the ignore rule was the culprit all along | Route recreated with the documented contract (session ownership, text XOR callback → 422, foreign bot hard 404, `error` always in body) and committed as part of V00.05.000; the ecosystem test that imports the module is the clean-checkout guard; the ignore rule is now root-anchored (`/test/`); the pre-commit procedure checks off every file a worklog entry names against `git status` AND runs `git check-ignore` on any file that fails to stage | FIXED — V00.05.000 |
| BR-022 | "Inline click buttons don't work in Telegram — they show nothing": button presses on live bots do nothing, and some bots stop answering entirely after edits | Investigation (production-mirror repro: real compiler → real store → real adapter → real webhook ingestion) proved the current pipeline round-trips buttons correctly — `reply_markup` renders, `callback_query` resolves, replies send. Two REAL defects found around it: (1) `loadCapabilities` parsed the bot's whole saved configuration all-or-nothing — ONE reply that no longer matched the schema (a rule written by an older compiler, a hand-edited row) emptied **every** reply: no /start, no buttons, a silently dead bot; (2) users had no Restart control to re-register a bot's Telegram webhook (new secret + current `allowed_updates`) after platform updates, so stale registrations could keep dropping `callback_query` updates until the owner republished | (1) `loadCapabilities` is now per-rule: invalid rules are dropped, valid ones keep running (regression test in `product.test.ts`); (2) new ownership-checked `POST /api/my/bots/[id]/restart` + **Run / Stop / Restart** buttons on the bot page — Restart is the documented "something is off" control that re-registers the webhook; (3) the dead-button toast now tells the user how to recover ("send /start to get fresh buttons"); (4) the button round-trip is covered by tests the e2e harness never had | FIXED — V00.08.002 |
| BR-023 | "Two menu buttons in the chat" and "the chat page shows the default page — users must click through to find previous chats via the second menu" (same on the agent page) | On phones, the site header's hamburger AND the chat view's own `☰ Chats`/`☰ Builds` trigger rendered stacked on one screen; worse, the chat trigger only existed while a chat was open — landing on `/chats` with no chat selected showed the empty state with no path to history (and no auto-open of the recent conversation), so returning users saw a blank default instead of their chats | One menu button per screen: chat surfaces pass `hideMobileMenu` to the site header and render the single drawer trigger in a slim top bar that exists on the empty state too; landing on `/chats` or `/chats/agents` with history now opens the most recent session immediately (`router.replace`, once per user, skipped for `?task=` deep links); the drawer carries the session list plus site navigation so hiding the header hamburger loses nothing | FIXED — V00.08.002 |

| BR-024 | The site cannot compile at all — a syntax error (`const obileOpen, setMobileOpen]`) shipped inside the committed `site-shell.tsx` at V00.08.002, so `npm run build`/`dev` fail from a fresh checkout | A corrupted edit slipped through the previous session's commit; the "build PASS" verification claim did not hold for the shipped tree | Line restored to `const [mobileOpen, setMobileOpen] = useState(false)`; the full verify gate (tsc + build before commit) is re-affirmed as the process guard | FIXED — V00.09.000 |
| BR-025 | "The site doesn't properly parse tool calls and the output from those tool calls": raw model JSON leaked into chat bubbles when the agent's JSON envelope was malformed, and tool RESULTS were invisible in the UI (activity chips showed only a one-line label; outputs vanished entirely after a reload) | `parseAgentReply` fell back to showing the whole raw text when `JSON.parse` failed, and the persisted activity metadata stored no tool output at all | Parser hardened (BR-025): code fences stripped, broken envelopes salvaged for the `message` field (closed OR truncated), prose outside braces shown alone, and a clean notice replaces pure garbage — raw JSON can never reach a user again; tool outputs persist as `dataPreview` in the audit metadata, and the shared `AgentActivity` component (agents view + Operator console) renders every step EXPANDABLE with its real output | FIXED — V00.09.000 |
| BR-026 | "The bot owner doesn't know about an order until he checks the site" — a restaurant-bot customer orders and the owner has no fast path to hear about it | Bots had no owner-notification path at all; the audience data existed (`/api/my/bots/[id]/users`) but nothing pushed it to Telegram | **Instant owner alerts**: new `Bot.ownerChatId`; the owner wires their own Telegram chat id on the bot page (Save + Send test) or asks the agent (`bot_set_owner_chat` + the order-form-alerts skill); the pipeline pushes a Telegram message the moment a collect flow (order/intake form) completes or a Stars payment lands; delivery is logged (`OWNER_NOTIFIED` / `OWNER_NOTIFY_FAILED`), best-effort, and verified by pipeline-level tests | FIXED — V00.09.000 |
| BR-027 | "The admin can't monitor or stop USER bots — where are the logs of user-run bots?" | `/api/admin/bots` did not exist and the admin console had no view of customer bots (only the official fleet); lifecycle overrides existed on the API but were undiscoverable without the bot id | New `GET /api/admin/bots` (every bot + owner identity + status + audience + message volume, admin-token guarded) and an `/admin/bots` monitor view with Start/Stop/Restart for any bot (existing admin-guarded `/api/bots/[id]/*` lifecycle routes power it); the customers table gained bot counts | FIXED — V00.09.000 |
| BR-028 | "/admin is just one page — where are /admin/dashboard and /admin/customers? Users don't have a dashboard either; /pricing vanished from the navigation; the site should live at /<username>_<uid>/dashboard" | The admin console was a single SPA at `/admin` with internal state; users had no dashboard surface; the signed-in nav omitted Pricing; no vanity routes existed | Real admin routes (`/admin/dashboard`, `/admin/bots`, `/admin/customers`, `/admin/agent`, `/admin/settings`, old `/admin` redirects, URL stays honest during SPA navigation); a full USER dashboard at `/dashboard` AND the vanity route `/<username>_<uid>/dashboard` (slug utils + session-checked resolution); `/chat` redirects to `/dashboard`; **Dashboard** and **Pricing** added to the signed-in nav | FIXED — V00.09.000 |
| BR-030 | "The site doesn't properly parse tool calls" (live proof): agent chats displayed raw markup — Qwen 'tool_call' tags around a bare tool name, CommandR/Hermes heredoc brackets with python-style arguments (bot_create_draft died as "Invalid arguments"), GLM/MCP 'invoke' wrappers (sometimes emitted as the bare word "mcp:tool"), and dots_function_call wrappers | The agent loops understood ONLY the strict JSON envelope; any other tool-call dialect passed through unparsed into the chat bubble, and python-literal arguments were never even attempted | New universal markup parser: every dialect becomes a real registry call (a recursive python-literal parser handles nested lists/dicts/escapes), executed with results fed back and rendered in the activity feed; ALL dialect residue is stripped from the visible text; markup inside code fences is treated as documentation, never executed; both agent prompts now explicitly forbid emitting markup | FIXED — V00.09.001 |
| BR-031 | /admin/customers answered "Internal server error" and showed "No customers yet" on a live deployment (V00.09.000) | The deployment updated its code but never ran `npx prisma db push`; the stale SQLite schema made a route subquery throw, and one failed OPTIONAL subquery killed the entire directory response; nothing on boot ever migrated the database | Two layers: (1) boot-time zero-touch schema sync — NURAE now runs `prisma db push --skip-generate` against its own sqlite database on startup (NURAE_AUTO_MIGRATE=0 opts out; non-file databases are never auto-pushed; destructive changes still refuse); (2) the customers route degrades OPTIONAL enrichments (bot counts, chat volume) to zeros instead of failing — the core customer list stays live | FIXED — V00.09.001 |
| BR-029 | Owner decision: REMOVE the admin's power to delete a user account (previously requested, then reversed after the cascade was built) | The cascade delete (`DELETE /api/admin/customers/[id]`, trash button + confirm dialog, `nuraeApi.deleteCustomer`) gave the operator account-termination powers the owner does not want the admin to have | Route file deleted, UI button + dialog removed, client function removed; the customers directory is now explicitly read-only ("see and monitor, never delete"); a regression test asserts the route module is gone and stays gone | FIXED — V00.09.000 |
| BR-032 | Full-suite `bun test` runs **wrote test fixtures into the developer's real database** (`db/custom.db`) — fake users (t24-*), fixture bots, test projects appeared in the live admin console, and full-suite failures fluctuated randomly (12–37) between runs | `tests/nurae/helpers.ts` set `process.env.DATABASE_URL` to an isolated temp DB, but several test files (bot-manager, gateway, telegram, schema-sync, …) imported NURAE src modules **without importing helpers first**; bun evaluates modules process-wide and caches them, so the first file whose import chain reached `src/lib/db.ts` constructed `PrismaClient` while env still pointed at the project `.env` — and that dev-DB-bound client was then shared with every later file | `import './helpers'` is now the FIRST import of **every** test file (10 files gained it), guaranteeing the isolated env is in place before any NURAE module evaluates; the dev database was cleaned of leaked fixtures; verified: dev DB row counts are byte-identical before/after a full suite run | FIXED — V00.09.002 |
| BR-033 | Full-suite runs still fail 2 tests that pass in isolation ("Plus ×3 boosts the free daily allowance", "customers directory degrade BR-031") | bun shares ONE module instance of `helpers.ts` and of the PrismaClient across all test files, so all files share a single temp database; `pushTestSchema()` now force-resets that shared DB per file (fixing 20 of the cross-file failures), but two tests depend on module-level in-memory state (pricing caches / client bindings) seeded by files that ran earlier — a per-file DB cannot help while the client is a process-global | Mitigated: per-file `--force-reset` on the shared DB (BR-032's fix) plus first-import helpers; the two residual failures are documented, pass in isolation, and their full fix requires restructuring `src/lib/db.ts` into a per-context factory or running each test file in its own process — deferred to a dedicated test-infra round | OPEN — V00.09.002 |

## Notes
- Every fix above ships with regression coverage in `tests/nurae/` — the
  suite is 349/351 in full runs (BR-033 documents the two documented
  cross-process-cache tests; both pass in isolation) across 24 files as of
  V00.09.002-beta-03 (page-level UI changes are
  additionally verified in a real browser; React components have no vitest
  harness yet).
- BR-004 evidence: with the owner-provided Gmail app password, a real
  verification-template email was delivered from the sandbox (`250 2.0.0 OK`).
  If mail still doesn't arrive on the device after updating: check spam, use
  the **newest** code (old ones are invalidated), and confirm the two
  `NURAE_GMAIL_*` variables in `.env` — see SETUP.md §10 Troubleshooting.
- Credentials shared in chat should be rotated once testing is done
  (Google Account → Security → App passwords), and the OpenRouter key that
  was exposed earlier should be rotated as well.
