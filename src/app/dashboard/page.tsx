'use client';

import { UserDashboardView } from '@/components/nurae/user-dashboard';

/** NURAE — /dashboard: the signed-in user's morning view. */
export default function Page() {
  return <UserDashboardView />;
}
