import { BillingView } from '@/components/nurae/billing-view';

/** NURAE — /billing: the pay-as-you-use wallet (balance, topups, ledger). */
export default function Page() {
  return <BillingView />;
}
