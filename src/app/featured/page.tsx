import { FeaturedView } from '@/components/nurae/featured-view';

/** NURAE — /featured: the featured conversation, kept deliberately small. */
export default function Page() {
  return <FeaturedView />;
}
