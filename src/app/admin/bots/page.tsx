import { NuraeConsole } from '@/components/nurae/console';

/** NURAE admin — /admin/bots (real route for the bots section). */
export default function Page() {
  return <NuraeConsole initialSection="bots" />;
}
