import { NuraeConsole } from '@/components/nurae/console';

/** NURAE admin — /admin/customers (real route for the customers section). */
export default function Page() {
  return <NuraeConsole initialSection="customers" />;
}
