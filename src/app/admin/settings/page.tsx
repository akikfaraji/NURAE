import { NuraeConsole } from '@/components/nurae/console';

/** NURAE admin — /admin/settings (real route for the settings section). */
export default function Page() {
  return <NuraeConsole initialSection="settings" />;
}
