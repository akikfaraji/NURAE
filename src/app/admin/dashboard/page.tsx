import { NuraeConsole } from '@/components/nurae/console';

/** NURAE admin — /admin/dashboard (real route for the dashboard section). */
export default function Page() {
  return <NuraeConsole initialSection="dashboard" />;
}
