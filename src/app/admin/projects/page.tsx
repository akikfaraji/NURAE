import { NuraeConsole } from '@/components/nurae/console';

/** NURAE admin — /admin/projects (real route for the projects section). */
export default function Page() {
  return <NuraeConsole initialSection="projects" />;
}
