import { BotDetailView } from '@/components/nurae/bot-detail-view';

/** NURAE — /bots/[id]: one bot (config, commands, replies, test, publish). */
export default function Page() {
  return <BotDetailView />;
}
